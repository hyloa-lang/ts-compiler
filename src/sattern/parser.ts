// Produces an AST.

import { Symbol } from ".";


// Produces an AST.
export class Parser {
  constructor(symbols: Set<Symbol>) {
    // TODO
  }
  
  parse<S extends Symbol>(str: string, symbol: S): InstanceType<S> {
    // TODO
  }
}
